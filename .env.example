# Production site URL (metadataBase, canonical, og:*, sitemap, robots, JSON-LD).
# Never set this to a *.vercel.app host – those are rejected and fall back to the production domain.
NEXT_PUBLIC_SITE_URL=https://wiener-entkernung.at
